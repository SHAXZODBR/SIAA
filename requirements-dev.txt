# Development / CI extras — not needed on a clinic install.
#   pip install -r requirements.txt -r requirements-dev.txt
#   python -m pytest -q
pytest>=8.0
httpx>=0.24            # fastapi.testclient transport
