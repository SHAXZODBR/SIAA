# === Core AI / ML ===
torch>=2.0
torchvision>=0.15
monai>=1.3
pydicom>=2.4
# DICOM compressed-syntax decoders — real scanners emit JPEG2000/JPEG-LS/RLE.
# Without these, pixel_array() raises on compressed studies (silent data loss).
pylibjpeg>=2.0
pylibjpeg-libjpeg>=2.1
pylibjpeg-openjpeg>=2.1
pylibjpeg-rle>=2.0
python-gdcm>=3.0
# Brain-MRI preprocessing (validated, commercial-safe / Apache-2.0) — zero-label
# accuracy wins that align clinical scans to model training distributions.
SimpleITK>=2.3          # N4 bias-field correction
HD-BET>=2.0             # skull-stripping (DKFZ, validated with pathology); optional, degrades gracefully
numpy>=1.24
opencv-python-headless>=4.8
Pillow>=10.0
scikit-learn>=1.3
scipy>=1.11
torchxrayvision>=1.2  # Pre-trained chest X-ray models (500K+ training images)

# === HuggingFace model stack (brain/head_ct/mammo/TB/pneumonia/COVID + MedSAM/MedGemma) ===
transformers>=4.40     # AutoImageProcessor, SamModel, AutoModelForImageTextToText
safetensors>=0.4       # loading SwinUNETR/MONAI safetensors weights
huggingface_hub>=0.20  # snapshot_download for datasets + gated weights
nibabel>=5.0           # NIfTI loading for BraTS / MSD validation

# === Fine-tuning ===
peft>=0.10             # LoRA adapters for MedGemma + brain classifier
accelerate>=0.28       # device_map='auto', training acceleration

# === HTTP / networking ===
requests>=2.28         # Ollama API + Orthanc PACS calls

# === Training & Tracking ===
mlflow>=2.9
tqdm>=4.66
matplotlib>=3.8

# === nnU-Net (optional, for custom 3D training) ===
nnunetv2>=2.2

# === Inference Server ===
fastapi>=0.104
uvicorn[standard]>=0.24
python-multipart>=0.0.6
pydantic>=2.5

# === Database ===
aiosqlite>=0.19

# === Security ===
cryptography>=41.0
bcrypt>=4.0            # password hashing (used directly in src/utils/auth.py)
python-jose[cryptography]>=3.3

# === Utilities ===
loguru>=0.7
pyyaml>=6.0
python-dotenv>=1.0
pandas>=2.0            # CSV indexing + dataset prep
